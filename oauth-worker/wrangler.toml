name = "stacknoah-auth"
main = "src/index.js"
compatibility_date = "2026-01-01"

# 비밀값은 여기 적지 말 것. 아래 명령으로 넣는다
#   npx wrangler secret put GITHUB_CLIENT_ID
#   npx wrangler secret put GITHUB_CLIENT_SECRET
